# Set Python3 environment
layout python3

# pip install func & check installed dir
PIPDIR='./.direnv/python-3.4.3/lib/python3.4/site-packages'
pin() {
    [[ ! -e $PIPDIR/$1 ]] && pip install $1==$1
}

<+CURSOR+>
# vim:set fenc=utf-8 ff=unix expandtab sw=4 ts=4:
